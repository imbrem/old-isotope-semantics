digraph {
    fontname = monospace
    node [shape=box, fontname=monospace]

    entry -> loop
    loop -> loop
    loop -> exit

    entry [label="'entry"]
    loop [label="'loop"]
    exit [label="'exit"]
}
